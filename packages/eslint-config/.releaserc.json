{
  "branches": [
    { "name": "master", "channel": "latest" },
    { "name": "release", "channel": "latest" },
    { "name": "rc", "channel": "rc", "prerelease": true },
    { "name": "beta", "channel": "beta", "prerelease": true },
    { "name": "alpha", "channel": "alpha", "prerelease": true },
    { "name": "dev", "channel": "development", "prerelease": true },
    { "name": "feature-*", "channel": "feature", "prerelease": true }
  ],
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "conventionalcommits",
        "releaseRules": [
          { "type": "cli-major", "release": "major" },
          { "type": "cli-minor", "release": "minor" },
          { "type": "cli-patch", "release": "patch" }
        ]
      }
    ],
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "CHANGELOG.md"],
        "message": "bump(main): Bumping eslint-config to ${nextRelease.version}"
      }
    ]
  ],
  "tagFormat": "v-eslint-config-${version}"
}
