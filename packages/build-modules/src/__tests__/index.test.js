/**
 * Test for the index file.
 * @file This file is saved as `index.test.js`.
 */

import '@testing-library/jest-dom';

describe('index', () => {
  it('should be true', () => {
    expect(true).toBe(true);
  });
});
